default: &default
  workers:
    - queues: "*"
      threads: 3
      polling_interval: 0.1
  dispatchers:
    - polling_interval: 1
      batch_size: 500

development:
  <<: *default

test:
  <<: *default
